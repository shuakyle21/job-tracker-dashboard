{
  "name": "Job Tracker — Gmail ingest",
  "nodes": [
    {
      "parameters": {
        "pollTimes": {
          "item": [
            {
              "mode": "everyX",
              "value": 15,
              "unit": "minutes"
            }
          ]
        },
        "simple": false,
        "maxResults": 25,
        "filters": {
          "q": "(label:job-application OR \"thank you for applying\" OR \"thanks for applying\" OR \"application has been received\" OR \"received your application\" OR \"successfully submitted\" OR \"your application was sent\" OR \"application sent to\" OR \"has viewed your application\" OR \"your application was viewed\" OR \"update on your application\" OR subject:\"your application\" OR subject:\"application received\") -label:job-application-processed -in:chats",
          "readStatus": "both",
          "includeSpamTrash": false,
          "includeDrafts": false
        },
        "options": {}
      },
      "type": "n8n-nodes-base.gmailTrigger",
      "typeVersion": 1.4,
      "position": [
        0,
        300
      ],
      "id": "gmail-trigger",
      "name": "Poll Gmail",
      "credentials": {
        "gmailOAuth2": {
          "id": "REPLACE_ME",
          "name": "Gmail account"
        }
      }
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        520
      ],
      "id": "backfill-trigger",
      "name": "Backfill (manual)"
    },
    {
      "parameters": {
        "resource": "message",
        "operation": "getAll",
        "returnAll": false,
        "limit": 500,
        "simple": false,
        "filters": {
          "q": "(label:job-application OR \"thank you for applying\" OR \"thanks for applying\" OR \"application has been received\" OR \"received your application\" OR \"successfully submitted\" OR \"your application was sent\" OR \"application sent to\" OR \"has viewed your application\" OR \"your application was viewed\" OR \"update on your application\" OR subject:\"your application\" OR subject:\"application received\") -label:job-application-processed -in:chats",
          "readStatus": "both",
          "includeSpamTrash": false
        },
        "options": {}
      },
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.2,
      "position": [
        0,
        720
      ],
      "id": "backfill-fetch",
      "name": "Fetch Job Mail",
      "credentials": {
        "gmailOAuth2": {
          "id": "REPLACE_ME",
          "name": "Gmail account"
        }
      }
    },
    {
      "parameters": {
        "operation": "removeItemsSeenInPreviousExecutions",
        "logic": "removeItemsWithAlreadySeenKeyValues",
        "dedupeValue": "={{ $json.id }}",
        "options": {
          "scope": "workflow",
          "historySize": 10000
        }
      },
      "type": "n8n-nodes-base.removeDuplicates",
      "typeVersion": 2,
      "position": [
        220,
        300
      ],
      "id": "dedupe",
      "name": "Skip Already Seen"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "language": "javaScript",
        "jsCode": "// Body of the \"Parse Job Email\" Code node (mode: runOnceForEachItem).\n//\n// This file is the source of truth. scripts/build-n8n.mjs inlines it into\n// n8n/job-tracker-ingest.json, and scripts/verify.mjs fails the build if the\n// two drift apart. Test it outside n8n with: node scripts/test-parser.mjs\n//\n// Contract: reads $json (one Gmail message), returns { json: { ...row } }.\n// It never throws — an email it cannot classify comes back with\n// parsed:false and is routed to the needs-review tab rather than dropped.\n\nconst msg = $json;\nconst subject = String(msg.subject || '');\nconst from = String(msg.from?.value?.[0]?.address || msg.from?.text || msg.From || '');\nconst fromName = String(msg.from?.value?.[0]?.name || '');\nconst body = String(msg.text || msg.textAsHtml || msg.snippet || '');\n\n// Subject carries the strongest signal and the least boilerplate, so weight it\n// by searching it first; fall back to the body only when the subject is mute.\nconst hay = (subject + '\\n' + body).toLowerCase();\nconst subjectLower = subject.toLowerCase();\n\nfunction any(text, patterns) {\n  for (const p of patterns) if (p.test(text)) return true;\n  return false;\n}\n\n// --- status classification ------------------------------------------------\n// Order is load-bearing. A rejection that follows an interview says both\n// \"interview\" and \"unfortunately\"; the current status is rejected, so the\n// rejection test must run before the interview test. Stage evidence is\n// collected separately below so the funnel still gets credit for the interview.\nconst RULES = [\n  ['offer', [\n    /\\boffer of employment\\b/, /\\bpleased to offer\\b/, /\\bjob offer\\b/,\n    /\\bwe(?:'| a)re offering you\\b/, /\\boffer letter\\b/,\n  ]],\n  ['rejected', [\n    /\\bunfortunately\\b/, /\\bregret to inform\\b/, /\\bnot (?:be )?moving forward\\b/,\n    /\\bnot (?:been )?select(?:ed|ing)\\b/, /\\bdecided to (?:proceed|move forward) with (?:other|another)\\b/,\n    /\\bposition has been filled\\b/, /\\bwe(?:'| ha)ve decided not to\\b/,\n    /\\bno longer under consideration\\b/, /\\bwill not be proceeding\\b/,\n  ]],\n  ['interview scheduled', [\n    /\\binterview (?:is |has been )?(?:scheduled|confirmed|booked)\\b/,\n    /\\binvit(?:ation|e|ing) (?:you )?(?:to|for) an? (?:interview|call|chat)\\b/,\n    /\\bschedule (?:an?|your) (?:interview|call)\\b/, /\\bcalendly\\.com\\b/,\n    /\\bbook a time\\b/, /\\bpick a time\\b/, /\\bavailability for a (?:call|chat|interview)\\b/,\n  ]],\n  ['assessment', [\n    /\\b(?:online |skills? |technical )?assessment\\b/, /\\btake[- ]home\\b/,\n    /\\bcoding (?:challenge|test|exercise)\\b/, /\\bhackerrank\\b/, /\\bcodility\\b/,\n    /\\btestgorilla\\b/, /\\bcomplete (?:a|the|this) (?:short )?test\\b/,\n  ]],\n  ['talent pool', [\n    /\\btalent (?:pool|community|network)\\b/, /\\bkeep your (?:resume|cv|profile) on file\\b/,\n    /\\bfuture (?:opportunities|openings|roles)\\b/,\n  ]],\n  ['viewed by employer', [\n    /\\byour application was viewed\\b/, /\\b(?:has )?viewed your application\\b/,\n    /\\bapplication (?:was )?viewed\\b/, /\\brecruiter viewed\\b/,\n  ]],\n  ['in review', [\n    /\\bunder review\\b/, /\\breviewing your application\\b/, /\\bbeing reviewed\\b/,\n    // \"You have been shortlisted\", not \"if shortlisted, we will call\" — the\n    // conditional is boilerplate in plain confirmations.\n    /\\b(?:you(?:'ve| have)? been|you are|you're) shortlisted\\b/, /\\bmoved to the next (?:stage|round)\\b/,\n    /\\bprogress(?:ed|ing) to\\b/,\n  ]],\n  ['applied', [\n    // Real confirmations don't agree on wording: JobStreet says \"was\n    // successfully submitted to\", LinkedIn \"was sent to\", Torre \"was\n    // delivered\", ATS mail \"has been received\" / \"we've received\". The\n    // optional \"for <title>\" lets \"application for X was submitted\" match too.\n    /\\bapplication (?:for .{1,120}? )?(?:was |has been )?(?:successfully )?(?:sent|submitted|received|delivered)\\b/,\n    /\\bsuccessfully submitted\\b/,\n    /\\bthank(?:s| you) for (?:applying|your application|your interest|your submission)\\b/,\n    /\\bwe(?:'| ha)ve received your application\\b/, /\\breceived your application\\b/,\n    /\\bconfirm that we have received\\b/, /\\bapplication confirmation\\b/,\n    /\\bsuccessfully applied\\b/,\n  ]],\n];\n\n// Gmail marks mail you sent with SENT. An outgoing email under the job label\n// is you applying directly — there is no reply to classify.\n// The trigger and the getAll node don't agree on the shape: labelIds is an\n// array of ids on some, labels an array of {id, name} on others. Read both.\nconst labelIds = [\n  ...(Array.isArray(msg.labelIds) ? msg.labelIds : []),\n  ...(Array.isArray(msg.labels) ? msg.labels.map((l) => (l && (l.id || l.name)) || '') : []),\n];\nconst isSent = labelIds.includes('SENT');\n\nlet status = '';\nif (isSent) status = 'applied';\nelse {\n  for (const [name, patterns] of RULES) {\n    if (any(subjectLower, patterns) || any(hay, patterns)) { status = name; break; }\n  }\n}\n\n// --- furthest stage reached ----------------------------------------------\n// A rejection tells you an application ended, not how far it got. If the same\n// email mentions an interview or an assessment, that is evidence the funnel\n// should credit — this is the max_stage column the dashboard reads, filled in\n// automatically instead of by hand.\nconst STAGE_OF_STATUS = {\n  'applied': 1, 'no response': 1, 'closed / expired': 1, 'rejected': 1,\n  'viewed by employer': 2, 'in review': 2, 'talent pool': 2,\n  'assessment': 3, 'interview scheduled': 4, 'interviewed': 4, 'offer': 5,\n};\n\nlet stageEvidence = 1;\nif (any(hay, [/\\breview(?:ed|ing)?\\b/, /\\bviewed\\b/, /\\bbeen shortlisted\\b/])) stageEvidence = 2;\nif (any(hay, [/\\bassessment\\b/, /\\btake[- ]home\\b/, /\\bcoding (?:challenge|test)\\b/])) stageEvidence = 3;\nif (any(hay, [/\\binterview(?:ed|ing)?\\b/, /\\bcalendly\\b/])) stageEvidence = 4;\nif (any(hay, [/\\boffer of employment\\b/, /\\boffer letter\\b/])) stageEvidence = 5;\n\nconst maxStage = Math.max(STAGE_OF_STATUS[status] || 1, stageEvidence);\n\n// status → Gmail sub-label name. scripts/build-n8n.mjs maps each name to a\n// label id, and verify.mjs fails if any name here has no id there.\nconst STATUS_LABELS = {\n  'applied': 'Applied', 'viewed by employer': 'Viewed', 'in review': 'In Review',\n  'assessment': 'Assessment', 'interview scheduled': 'Interview', 'offer': 'Offer',\n  'rejected': 'Rejected', 'talent pool': 'Talent Pool',\n};\n\n// --- company --------------------------------------------------------------\n// The From display name is the most reliable source, but ATS senders bury the\n// company in boilerplate. Strip the boilerplate rather than guess.\nlet company = fromName\n  .replace(/\\s+via\\s+LinkedIn\\s*$/i, '')\n  .replace(/^(?:no[- ]?reply|do[- ]?not[- ]?reply|notifications?|jobs?|careers?|recruiting|talent|hr)\\b[\\s@:-]*/i, '')\n  .replace(/\\b(?:careers?|recruiting|recruitment|talent(?:\\s+acquisition)?|hiring|hr|jobs?|team|no[- ]?reply)\\b/gi, '')\n  .replace(/[|()\\[\\]]/g, ' ')\n  .replace(/\\s{2,}/g, ' ')\n  .replace(/^[\\s,.\\-–—]+|[\\s,.\\-–—]+$/g, '')\n  .trim();\n\n// For mail you sent, the employer is the recipient, not you.\nconst to = String(msg.to?.value?.[0]?.address || msg.to?.text || '');\nif (isSent) company = '';\n\n// Fall back to the sending domain, minus the ATS hosts that tell you nothing\n// about who the employer is.\nconst ATS_DOMAINS = /^(?:.*\\.)?(?:linkedin|indeed|greenhouse|lever|workday(?:day)?|myworkday|jobstreet|glassdoor|ziprecruiter|smartrecruiters|ashbyhq|workable|workablemail|bamboohr|teamtailor-mail|teamtailor|manatal|applytojob|appsheet|kalibrr|torre|onlinejobs|gmail|googlemail|outlook|yahoo)\\./i;\nif (!company) {\n  const domain = ((isSent ? to : from).split('@')[1] || '').toLowerCase();\n  if (domain && !ATS_DOMAINS.test(domain + '.')) {\n    company = domain.replace(/\\.(?:com|net|org|io|co|ph|ai|dev|gov|edu)(?:\\.[a-z]{2})?$/i, '');\n    company = company.charAt(0).toUpperCase() + company.slice(1);\n  }\n}\n\n// --- job title ------------------------------------------------------------\n// Subjects follow a handful of shapes. Peel the known prefixes, then take what\n// is left up to the first separator.\n// Subject lines stack boilerplate: \"Invitation to interview — Full Stack\n// Developer at Q2Q\". One pass over the lead-ins is not enough, because\n// stripping one exposes the next, so run the list until it stops shrinking.\n// Each pattern also eats a trailing separator, otherwise the split below\n// mistakes the leftover boilerplate for the job title.\nconst LEAD_INS = [\n  /^(?:re|fwd?)\\s*:\\s*/i,\n  /^invitation (?:to|for) (?:an? )?(?:interview|call|chat)\\s*(?:for|with)?\\s*[:\\u2014\\u2013-]?\\s*/i,\n  /^(?:your )?application (?:for|to|update(?: for)?|status(?: for)?|received(?: for)?|confirmation(?: for)?)\\s*[:\\u2014\\u2013-]?\\s*/i,\n  /^thank(?:s| you) for (?:applying|your (?:application|interest))(?: (?:to|in|for|at))?\\s*[:\\u2014\\u2013-]?\\s*/i,\n  /^(?:next steps?|update|interview|assessment|invitation)\\s*(?:on|for|regarding|about)?\\s*(?:your )?(?:application)?\\s*[:\\u2014\\u2013-]\\s*/i,\n  /^you(?:r)? (?:have )?applied (?:to|for)\\s*[:\\u2014\\u2013-]?\\s*/i,\n];\n\nlet title = subject.trim();\nlet shrinking = true;\nlet passes = 0;\nwhile (shrinking && passes < 4) {\n  const before = title;\n  for (const re of LEAD_INS) title = title.replace(re, '').trim();\n  shrinking = title.length < before.length;\n  passes = passes + 1;\n}\n\nconst atMatch = title.match(/^(.+?)\\s+(?:at|@|with|-|–|—|\\|)\\s+(.+)$/);\nif (atMatch) {\n  title = atMatch[1].trim();\n  // The right-hand side is usually the company; trust it over a stripped\n  // display name, which is often just \"Careers\".\n  const candidate = atMatch[2].replace(/[|()\\[\\]].*$/, '').trim();\n  if (candidate && candidate.length < 60 && (!company || /^careers?$/i.test(company))) {\n    company = candidate;\n  }\n}\ntitle = title.replace(/\\s*[|(].*$/, '').replace(/\\s+(?:position|role)$/i, '').replace(/\\s{2,}/g, ' ').trim();\n\n// --- sender-specific shapes -----------------------------------------------\n// The heuristics above read the subject; these read sentences the big senders\n// actually use, which name both the job and the employer. When one matches it\n// is more reliable than anything guessed from a display name, so it wins.\n// Company names end in \"Inc.\" / \"Ltd.\" — keep that period, drop a sentence one.\nconst clean = (s) => String(s || '').replace(/\\s+/g, ' ').trim()\n  .replace(/(?<!\\b(?:inc|ltd|corp|co|llc))\\.$/i, '').trim();\n// A real job title is never a bare article/pronoun. Extractors with no anchor\n// past their keyword (e.g. \"...applying for <T> job\") can otherwise pick up a\n// stray \"a\"/\"the\" out of unrelated boilerplate, like a safety disclaimer.\nconst isPlausibleTitle = (s) => !/^(?:a|an|the|this|that|it)$/i.test(s);\nconst EXTRACTORS = [\n  // JobStreet: \"your application for <T> was successfully submitted to <C>\"\n  [body, /application for (.+?) was successfully submitted to (.+?)(?=\\s*(?:\\n|jobstreet\\b|$))/i, 1, 2],\n  // JobStreet: \"<C> has viewed your application for <T>\"\n  [subject, /^(.+?) has viewed your application for (.+)$/i, 2, 1],\n  // LinkedIn: subject names the company; the body's next line is the title.\n  [body, /your application was sent to ([^\\n]+)\\n+\\s*([^\\n]+)/i, 2, 1],\n  [subject, /your application was sent to (.+)$/i, 0, 1],\n  [subject, /your application was viewed by (.+)$/i, 0, 1],\n  // Kalibrr: \"Application sent to <T> at <C>!\"\n  [subject, /^application sent to (.+?) at (.+?)!?$/i, 1, 2],\n  // Indeed: the subject carries the title and nothing else.\n  [subject, /^indeed application:\\s*(.+)$/i, 1, 0],\n  // ATS / careers-page mail.\n  [body, /interest in (?:the )?(.+?) (?:position|role|opportunity) at ([^\\n!]+?)(?=[.!,]\\s|[.!]?\\s*$|\\n)/i, 1, 2],\n  [body, /(?:application|applying|apply) for (?:the )?(.+?) (?:role|position|job|opportunity) at ([^\\n!]+?)(?=[.!,]\\s|[.!]?\\s*$|\\n)/i, 1, 2],\n  // No anchor past the keyword here, so this also matches unrelated boilerplate\n  // — a safety disclaimer (\"never share your bank details when applying for a\n  // job\") reads as \"applying for <a> job\" and would otherwise capture the\n  // article \"a\" as the title. isPlausibleTitle() below rejects that.\n  [body, /(?:application|applying|apply) for (?:the )?(.+?) (?:role|position|job|opportunity)\\b/i, 1, 0],\n  // \"your application for <T> was delivered\" (Torre), \"...for <T>, and\" (Lever),\n  // \"...for <T> shortly\" (Teamtailor), \"...for <T>. If\" (Manatal).\n  [body, /your application for (?:the )?(.+?)(?= was\\b| shortly\\b| and\\b|,|\\.\\s|\\.?\\n|\\.?$)/i, 1, 0],\n  // \"Thank you for your interest in joining <C>, ...\" — names the employer\n  // when the sender is a person or an ATS.\n  [body, /interest in joining ([^,!.\\n]+)/i, 0, 1],\n];\nlet gotTitle = false;\nlet gotCompany = false;\nfor (const [text, re, ti, ci] of EXTRACTORS) {\n  const m = String(text).match(re);\n  if (!m) continue;\n  if (ti && !gotTitle && clean(m[ti]) && isPlausibleTitle(clean(m[ti]))) { title = clean(m[ti]); gotTitle = true; }\n  if (ci && !gotCompany && clean(m[ci])) { company = clean(m[ci]); gotCompany = true; }\n  if (gotTitle && gotCompany) break;\n}\n\n// --- job platform ---------------------------------------------------------\n// Where the application was made, for the Feed's Job Platform column. Job\n// boards are recognised by sending domain; anything else that wrote back\n// (ATS, careers page, recruiter) is the company's own channel.\nconst JOB_BOARDS = [\n  [/jobstreet/i, 'JobStreet'], [/linkedin/i, 'LinkedIn'], [/indeed/i, 'Indeed'],\n  [/kalibrr/i, 'Kalibrr'], [/onlinejobs/i, 'OnlineJobs.ph'], [/torre\\.ai/i, 'Torre'],\n];\nlet jobPlatform = 'Company Website';\nif (isSent) jobPlatform = 'Direct Email';\nelse for (const [re, name] of JOB_BOARDS) if (re.test(from)) { jobPlatform = name; break; }\nconst onJobBoard = JOB_BOARDS.some(([, name]) => name === jobPlatform);\n// A board's display name (\"Indeed Apply\", \"LinkedIn\") is not the employer.\nif (onJobBoard && !gotCompany) company = '';\n\n// --- confidence and routing ----------------------------------------------\n// A row is only trustworthy enough to land in the tracker when the email said\n// something recognisable AND we know who sent it. Everything else goes to\n// needs-review, where a human decides in ten seconds. The one exception: a job\n// board confirmation (Indeed) that names the job but not the employer is still\n// traceable, because the board itself identifies where the application lives.\nconst parsed = Boolean(status) && Boolean(title) && (Boolean(company) || onJobBoard);\n\nlet confidence = 'low';\nif (status && company && title) confidence = 'high';\nelse if (status && (company || title)) confidence = 'medium';\n\n// Gmail gives an RFC-2822 date on some shapes and epoch milliseconds on\n// others, and a malformed header gives neither. new Date(garbage).toISOString()\n// throws, which would fail the execution and leave the email unlabelled — so it\n// would be re-polled and re-fail forever. Validate before formatting.\nconst receivedAt = msg.date || msg.internalDate;\nconst stamp = /^\\d+$/.test(String(receivedAt)) ? Number(receivedAt) : receivedAt;\nconst asDate = receivedAt ? new Date(stamp) : new Date();\nconst isoDate = Number.isNaN(asDate.getTime())\n  ? new Date().toISOString().slice(0, 10)\n  : asDate.toISOString().slice(0, 10);\n\nconst threadId = String(msg.threadId || '');\nconst norm = (s) => String(s).toLowerCase().replace(/\\s+/g, ' ').trim();\n\nreturn {\n  json: {\n    message_id: String(msg.id || msg.messageId || ''),\n    thread_id: threadId,\n    received_at: isoDate,\n    company,\n    job_title: title,\n    status,\n    max_stage: maxStage,\n    source: /linkedin/i.test(from) ? 'LinkedIn'\n      : /indeed/i.test(from) ? 'Indeed'\n      : /jobstreet/i.test(from) ? 'JobStreet'\n      : /onlinejobs/i.test(from) ? 'OnlineJobs.ph'\n      : 'Email',\n    job_platform: jobPlatform,\n    // One Feed row per application: every email about the same job at the\n    // same employer lands on the same key. Where the employer is unknown\n    // (Indeed, or any board whose sentence-extractors missed the company),\n    // the board stands in for the employer, and the Gmail thread id is\n    // appended as a disambiguator — otherwise two different real employers\n    // that share a board and a common title (e.g. two \"Software Engineer\"\n    // applications via Indeed) would collapse onto the same Feed row and\n    // silently blend each other's dates/status. This does NOT run when the\n    // company is known: known-company applications must keep exactly\n    // `company|title` so that later emails about the *same* application\n    // (different thread, different received_at) still collapse onto the one\n    // row the forward-merge (merge-feed.js) depends on. Trade-off: if a board\n    // answers with a new Gmail thread instead of replying in the original\n    // one, this produces a second, disconnected Feed row for what is really\n    // the same application — accepted as a much safer failure mode than\n    // silently merging two unrelated applications.\n    application_key: company\n      ? norm(company) + '|' + norm(title)\n      : norm(jobPlatform) + '|' + norm(title) + (threadId ? '|' + threadId : ''),\n    // The Gmail sub-label under \"Job Application/\". Anything routed to\n    // needs-review is labelled that, whatever status was guessed.\n    status_label: parsed ? (STATUS_LABELS[status] || 'Needs Review') : 'Needs Review',\n    from_address: from,\n    subject,\n    parsed,\n    confidence,\n    // Subject + body, bounded, for the LLM fallback's prompt only. Never\n    // written to Airtable (INBOX_FIELDS/REVIEW_FIELDS don't list it) and\n    // never reaches dist/.\n    raw_text: (subject + '\\n\\n' + body).slice(0, 4000),\n  },\n};\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        440,
        300
      ],
      "id": "parse",
      "name": "Parse Job Email"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "not-parsed",
              "leftValue": "={{ $json.parsed }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "false",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        440,
        460
      ],
      "id": "needs-fallback",
      "name": "Needs LLM Fallback?"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://llmrouter.boyemma.com/v1/chat/completions",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpBearerAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({\n        model: \"auto\",\n        messages: [\n          { role: \"system\", content: \"You classify job-application emails. Reply with only a JSON object: {\\\"status\\\": one of applied, viewed by employer, in review, assessment, interview scheduled, offer, rejected, talent pool}. If none fit, reply {\\\"status\\\": \\\"\\\"}.\" },\n          { role: \"user\", content: $('Parse Job Email').item.json.raw_text },\n        ],\n      }) }}",
        "options": {
          "timeout": 15000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.5,
      "position": [
        620,
        460
      ],
      "id": "llm-classify",
      "name": "Classify With LLM",
      "onError": "continueRegularOutput",
      "credentials": {
        "httpBearerAuth": {
          "id": "REPLACE_ME",
          "name": "LLM Router Auth"
        }
      }
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "language": "javaScript",
        "jsCode": "// Body of the \"Apply LLM Fallback\" Code node (mode: runOnceForEachItem).\n//\n// This file is the source of truth. scripts/build-n8n.mjs inlines it into\n// n8n/job-tracker-ingest.json, and scripts/verify.mjs fails the build if the\n// two drift apart. Test it outside n8n with: node scripts/test-llm-fallback.mjs\n//\n// Contract: only reached when Parse Job Email's rules couldn't classify an\n// email. Reads $json (the \"Classify With LLM\" HTTP Request node's response)\n// and $('Parse Job Email').item.json (the original row), returns { json }\n// in the exact same shape parse-email.js produces. It never throws — a\n// malformed LLM reply, an unrecognized status, or an n8n error-passthrough\n// object all fall through to the original, unchanged parsed:false row rather\n// than guess. An LLM success can only improve a row, never regress one: it\n// is capped at confidence:'medium' (never 'high', since it's inferred, not\n// pattern-matched) and it never overrides an already-parsed row.\n\nconst original = $('Parse Job Email').item.json;\n\n// Mirrors n8n/parse-email.js's STATUS_LABELS exactly — scripts/verify.mjs\n// checks the two stay in sync.\nconst STATUS_LABELS = {\n  'applied': 'Applied', 'viewed by employer': 'Viewed', 'in review': 'In Review',\n  'assessment': 'Assessment', 'interview scheduled': 'Interview', 'offer': 'Offer',\n  'rejected': 'Rejected', 'talent pool': 'Talent Pool',\n};\n\nlet status = original.status;\nlet status_label = original.status_label;\nlet parsed = original.parsed;\nlet confidence = original.confidence;\n\nif (!parsed) {\n  try {\n    const guess = JSON.parse($json?.choices?.[0]?.message?.content ?? '');\n    const candidate = String(guess?.status || '').toLowerCase().trim();\n    // A board's own confirmation is traceable even without a named employer\n    // (Indeed) — same bar parse-email.js uses for `parsed`.\n    const onJobBoard = !['Company Website', 'Direct Email'].includes(original.job_platform);\n    if (Object.prototype.hasOwnProperty.call(STATUS_LABELS, candidate)\n        && original.job_title && (original.company || onJobBoard)) {\n      status = candidate;\n      status_label = STATUS_LABELS[status];\n      parsed = true;\n      confidence = 'medium';\n    }\n  } catch {\n    // Malformed LLM output, an n8n error-passthrough object (onError:\n    // continueRegularOutput), or an unrecognized status — leave the\n    // rule-based result untouched rather than guess.\n  }\n}\n\nreturn { json: { ...original, status, status_label, parsed, confidence } };\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        800,
        460
      ],
      "id": "llm-apply",
      "name": "Apply LLM Fallback"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "is-parsed",
              "leftValue": "={{ $json.parsed }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        660,
        300
      ],
      "id": "route",
      "name": "Classified?"
    },
    {
      "parameters": {
        "authentication": "airtableTokenApi",
        "resource": "record",
        "operation": "upsert",
        "base": {
          "__rl": true,
          "mode": "id",
          "value": "appRlqw66Sxzjpnf6"
        },
        "table": {
          "__rl": true,
          "mode": "id",
          "value": "tbl2ASezOoTqS4qok"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "matchingColumns": [
            "message_id"
          ],
          "value": {
            "message_id": "={{ $json[\"message_id\"] }}",
            "thread_id": "={{ $json[\"thread_id\"] }}",
            "received_at": "={{ $json[\"received_at\"] }}",
            "company": "={{ $json[\"company\"] }}",
            "job_title": "={{ $json[\"job_title\"] }}",
            "status": "={{ $json[\"status\"] }}",
            "max_stage": "={{ $json[\"max_stage\"] }}",
            "source": "={{ $json[\"source\"] }}",
            "confidence": "={{ $json[\"confidence\"] }}",
            "job_platform": "={{ $json[\"job_platform\"] }}"
          },
          "schema": [
            {
              "id": "message_id",
              "displayName": "message_id",
              "required": false,
              "defaultMatch": true,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "thread_id",
              "displayName": "thread_id",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "received_at",
              "displayName": "received_at",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "company",
              "displayName": "company",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "job_title",
              "displayName": "job_title",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "status",
              "displayName": "status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "max_stage",
              "displayName": "max_stage",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true
            },
            {
              "id": "source",
              "displayName": "source",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "confidence",
              "displayName": "confidence",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "job_platform",
              "displayName": "job_platform",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            }
          ]
        },
        "options": {
          "typecast": true
        }
      },
      "type": "n8n-nodes-base.airtable",
      "typeVersion": 2.1,
      "position": [
        900,
        180
      ],
      "id": "airtable-tbl2ASezOoTqS4qok",
      "name": "Write to Inbox",
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "credentials": {
        "airtableTokenApi": {
          "id": "REPLACE_ME",
          "name": "Airtable account"
        }
      }
    },
    {
      "parameters": {
        "authentication": "airtableTokenApi",
        "resource": "record",
        "operation": "upsert",
        "base": {
          "__rl": true,
          "mode": "id",
          "value": "appRlqw66Sxzjpnf6"
        },
        "table": {
          "__rl": true,
          "mode": "id",
          "value": "tblwEyImuS3hgYGFK"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "matchingColumns": [
            "message_id"
          ],
          "value": {
            "message_id": "={{ $json[\"message_id\"] }}",
            "thread_id": "={{ $json[\"thread_id\"] }}",
            "received_at": "={{ $json[\"received_at\"] }}",
            "from_address": "={{ $json[\"from_address\"] }}",
            "subject": "={{ $json[\"subject\"] }}",
            "status": "={{ $json[\"status\"] }}",
            "company": "={{ $json[\"company\"] }}",
            "job_title": "={{ $json[\"job_title\"] }}",
            "confidence": "={{ $json[\"confidence\"] }}"
          },
          "schema": [
            {
              "id": "message_id",
              "displayName": "message_id",
              "required": false,
              "defaultMatch": true,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "thread_id",
              "displayName": "thread_id",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "received_at",
              "displayName": "received_at",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "from_address",
              "displayName": "from_address",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "subject",
              "displayName": "subject",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "status",
              "displayName": "status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "company",
              "displayName": "company",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "job_title",
              "displayName": "job_title",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "confidence",
              "displayName": "confidence",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            }
          ]
        },
        "options": {
          "typecast": true
        }
      },
      "type": "n8n-nodes-base.airtable",
      "typeVersion": 2.1,
      "position": [
        900,
        420
      ],
      "id": "airtable-tblwEyImuS3hgYGFK",
      "name": "Write to Needs Review",
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "credentials": {
        "airtableTokenApi": {
          "id": "REPLACE_ME",
          "name": "Airtable account"
        }
      }
    },
    {
      "parameters": {
        "batchSize": 1
      },
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        1120,
        180
      ],
      "id": "loop-feed-rows",
      "name": "Loop Feed Rows"
    },
    {
      "parameters": {
        "authentication": "airtableTokenApi",
        "resource": "record",
        "operation": "search",
        "base": {
          "__rl": true,
          "mode": "id",
          "value": "appRlqw66Sxzjpnf6"
        },
        "table": {
          "__rl": true,
          "mode": "id",
          "value": "tblPZSpJ8P4iFuAhX"
        },
        "filterByFormula": "={{ '{Application Key} = ' + JSON.stringify($('Parse Job Email').item.json.application_key) }}",
        "returnAll": false,
        "limit": 1,
        "options": {}
      },
      "type": "n8n-nodes-base.airtable",
      "typeVersion": 2.1,
      "position": [
        1340,
        40
      ],
      "id": "find-feed-row",
      "name": "Find Feed Row",
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "credentials": {
        "airtableTokenApi": {
          "id": "REPLACE_ME",
          "name": "Airtable account"
        }
      }
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "language": "javaScript",
        "jsCode": "// Body of the \"Merge Into Feed\" Code node (mode: runOnceForEachItem).\n//\n// Source of truth, like parse-email.js: scripts/build-n8n.mjs inlines it and\n// scripts/verify.mjs fails if the two drift. Test with: node scripts/test-merge-feed.mjs\n//\n// Contract: $json is the Feed row \"Find Feed Row\" returned for this email's\n// application_key ({} when there is none); $('Parse Job Email').item.json is\n// the parsed email. Returns every Feed field the upsert writes, so the upsert\n// never blanks a value it wasn't meant to touch.\n//\n// Emails arrive in any order and a person also edits Feed by hand, so the\n// merge only ever moves a row forward: earliest date, highest stage, and a\n// status that doesn't slide back from \"Interviewed\" to \"Applied\" because a\n// late confirmation email turned up.\n\nconst email = $('Parse Job Email').item.json;\nconst row = ($json && ($json.fields || $json)) || {};\n\nconst FEED_STATUS = {\n  'applied': 'Applied', 'viewed by employer': 'Viewed by Employer', 'in review': 'In Review',\n  'assessment': 'Assessment', 'interview scheduled': 'Interviewed', 'offer': 'Offer',\n  'rejected': 'Rejected', 'talent pool': 'Talent Pool',\n};\n\n// How far along a status is, for \"may this replace that?\". Not the funnel\n// stage: a rejection ends an application at any stage, so it outranks\n// everything except an offer.\nconst RANK = {\n  'Materials Ready — VERIFY': 0, 'Applied': 1, 'No Response': 1,\n  'Viewed by Employer': 2, 'In Review': 2, 'Talent Pool': 3, 'Assessment': 3,\n  'Interviewed': 4, 'Closed / Expired': 5, 'Rejected': 5, 'Offer': 6,\n};\n\n// Feed's Source select predates Job Platform and uses its own spellings.\nconst SOURCE_OF_PLATFORM = {\n  'JobStreet': 'Jobstreet', 'LinkedIn': 'LinkedIn', 'Indeed': 'Indeed', 'Kalibrr': 'Kalibrr',\n  // Only choices Source already has: a new option would silently add a\n  // channel to the dashboard. OnlineJobs.ph has none, so it is a \"Platform\".\n  'OnlineJobs.ph': 'Platform', 'Torre': 'Torre.ai', 'Company Website': 'Direct / Careers',\n  'Direct Email': 'Direct Email',\n};\n\nconst oldStatus = row['Status'] || '';\nconst newStatus = FEED_STATUS[email.status] || '';\nconst status = !oldStatus || (newStatus && (RANK[newStatus] ?? 0) >= (RANK[oldStatus] ?? 0))\n  ? (newStatus || oldStatus)\n  : oldStatus;\n\nconst dates = [row['Date Applied'], email.received_at].filter(Boolean).sort();\n\nreturn {\n  json: {\n    'Application Key': email.application_key,\n    'Job Role': row['Job Role'] || email.job_title,\n    'Company': row['Company'] || email.company,\n    'Job Platform': row['Job Platform'] || email.job_platform,\n    'Source': row['Source'] || SOURCE_OF_PLATFORM[email.job_platform] || 'Direct / Careers',\n    'Date Applied': dates[0] || '',\n    'Status': status,\n    'Max Stage': Math.max(Number(row['Max Stage']) || 0, Number(email.max_stage) || 1),\n  },\n};\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1340,
        320
      ],
      "id": "merge-feed",
      "name": "Merge Into Feed"
    },
    {
      "parameters": {
        "authentication": "airtableTokenApi",
        "resource": "record",
        "operation": "upsert",
        "base": {
          "__rl": true,
          "mode": "id",
          "value": "appRlqw66Sxzjpnf6"
        },
        "table": {
          "__rl": true,
          "mode": "id",
          "value": "tblPZSpJ8P4iFuAhX"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "matchingColumns": [
            "Application Key"
          ],
          "value": {
            "Application Key": "={{ $json[\"Application Key\"] }}",
            "Job Role": "={{ $json[\"Job Role\"] }}",
            "Company": "={{ $json[\"Company\"] }}",
            "Job Platform": "={{ $json[\"Job Platform\"] }}",
            "Source": "={{ $json[\"Source\"] }}",
            "Date Applied": "={{ $json[\"Date Applied\"] }}",
            "Status": "={{ $json[\"Status\"] }}",
            "Max Stage": "={{ $json[\"Max Stage\"] }}"
          },
          "schema": [
            {
              "id": "Application Key",
              "displayName": "Application Key",
              "required": false,
              "defaultMatch": true,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Job Role",
              "displayName": "Job Role",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Company",
              "displayName": "Company",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Job Platform",
              "displayName": "Job Platform",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Source",
              "displayName": "Source",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Date Applied",
              "displayName": "Date Applied",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Status",
              "displayName": "Status",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            },
            {
              "id": "Max Stage",
              "displayName": "Max Stage",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true
            }
          ]
        },
        "options": {
          "typecast": true
        }
      },
      "type": "n8n-nodes-base.airtable",
      "typeVersion": 2.1,
      "position": [
        1600,
        180
      ],
      "id": "airtable-tblPZSpJ8P4iFuAhX",
      "name": "Upsert Feed",
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "credentials": {
        "airtableTokenApi": {
          "id": "REPLACE_ME",
          "name": "Airtable account"
        }
      }
    },
    {
      "parameters": {
        "resource": "message",
        "operation": "addLabels",
        "messageId": "={{ $('Parse Job Email').item.json.message_id }}",
        "labelIds": [
          "={{ $('Parse Job Email').item.json.parsed ? \"Label_1523943501121152783\" : \"Label_13\" }}",
          "Label_13",
          "={{ ({\"Applied\":\"Label_14\",\"Viewed\":\"Label_15\",\"In Review\":\"Label_16\",\"Assessment\":\"Label_17\",\"Interview\":\"Label_18\",\"Offer\":\"Label_19\",\"Rejected\":\"Label_20\",\"Talent Pool\":\"Label_21\",\"Needs Review\":\"Label_22\"})[$('Parse Job Email').item.json.status_label] || \"Label_22\" }}"
        ]
      },
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.2,
      "position": [
        1800,
        300
      ],
      "id": "mark-handled",
      "name": "Mark Email Processed",
      "onError": "continueRegularOutput",
      "credentials": {
        "gmailOAuth2": {
          "id": "REPLACE_ME",
          "name": "Gmail account"
        }
      }
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "hours",
              "hoursInterval": 1
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        940
      ],
      "id": "backlog-check",
      "name": "Ingest Backlog Check"
    },
    {
      "parameters": {
        "resource": "message",
        "operation": "getAll",
        "returnAll": false,
        "limit": 1,
        "simple": true,
        "filters": {
          "q": "=(label:job-application OR \"thank you for applying\" OR \"thanks for applying\" OR \"application has been received\" OR \"received your application\" OR \"successfully submitted\" OR \"your application was sent\" OR \"application sent to\" OR \"has viewed your application\" OR \"your application was viewed\" OR \"update on your application\" OR subject:\"your application\" OR subject:\"application received\") -label:job-application-processed -in:chats before:{{ Math.floor(Date.now() / 1000) - 2 * 3600 }}",
          "readStatus": "both",
          "includeSpamTrash": false
        },
        "options": {}
      },
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.2,
      "position": [
        220,
        940
      ],
      "id": "backlog-fetch",
      "name": "Find Stale Job Mail",
      "credentials": {
        "gmailOAuth2": {
          "id": "REPLACE_ME",
          "name": "Gmail account"
        }
      }
    },
    {
      "parameters": {
        "errorMessage": "Job mail older than 2h is still missing Job Application/Processed — the ingest is stuck. Check the Poll Gmail executions, then run Backfill (manual). If that doesn't clear it, the email was already seen once and Skip Already Seen drops it: find its row in Inbox/Feed, then label it Job Application/Processed by hand, or clear Skip Already Seen's deduplication history and backfill again. See DEPLOY.md §3.7."
      },
      "type": "n8n-nodes-base.stopAndError",
      "typeVersion": 1,
      "position": [
        440,
        940
      ],
      "id": "backlog-alarm",
      "name": "Stale Job Mail Alarm",
      "executeOnce": true
    },
    {
      "parameters": {
        "content": "## 1. Poll and dedupe\nQuery: mail under **Job Application**, or containing confirmation phrases (\"thank you for applying\", \"received your application\", …), minus **Job Application/Processed**.\n\n**Backfill (manual)**: run once to process mail already in the label.\n\n**Remove Duplicates** is the second net: it catches a re-delivery in the window between the Airtable write and the label being applied.",
        "width": 400,
        "height": 280,
        "color": 4
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -40,
        20
      ],
      "id": "sticky-19",
      "name": "Note 19"
    },
    {
      "parameters": {
        "content": "## 2. Classify\nPure function, no network. Source of truth is `n8n/parse-email.js` in the repo — it has tests. Edit it there and re-run `node scripts/build-n8n.mjs`, not here.",
        "width": 380,
        "height": 220,
        "color": 3
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        400,
        60
      ],
      "id": "sticky-20",
      "name": "Note 20"
    },
    {
      "parameters": {
        "content": "## 3. Store\nInbox / Needs Review rows key on `message_id`, so re-running is an update, not a duplicate.\n\nClassified mail also upserts **Feed**, one row per application (`Application Key` = company|title, or platform|title|thread-id when the employer is unknown). `merge-feed.js` only moves a row forward: earliest date, highest stage, no status regressions — hand edits survive.",
        "width": 860,
        "height": 260,
        "color": 5
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        820,
        20
      ],
      "id": "sticky-21",
      "name": "Note 21"
    },
    {
      "parameters": {
        "content": "## 5. Watchdog\nHourly: any job mail older than 2h that still lacks **Job Application/Processed** fails this execution. `scripts/check-live.mjs` (hourly in GitHub Actions) turns failed executions into an email, so a stuck ingest can't stay quiet.",
        "width": 560,
        "height": 160,
        "color": 2
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -40,
        1060
      ],
      "id": "sticky-22",
      "name": "Note 22"
    },
    {
      "parameters": {
        "content": "## 4. Close the loop\nLabel is applied **after** the write — a crash loses a label, not a row.\n\nNo rebuild here: every Feed upsert pings the **Feed change → rebuild** workflow, which dispatches to GitHub Actions, so the dashboard updates in seconds instead of waiting for the 6-hourly cron.",
        "width": 400,
        "height": 280,
        "color": 6
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1760,
        20
      ],
      "id": "sticky-23",
      "name": "Note 23"
    }
  ],
  "connections": {
    "Poll Gmail": {
      "main": [
        [
          {
            "node": "Skip Already Seen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Backfill (manual)": {
      "main": [
        [
          {
            "node": "Fetch Job Mail",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Job Mail": {
      "main": [
        [
          {
            "node": "Skip Already Seen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Skip Already Seen": {
      "main": [
        [
          {
            "node": "Parse Job Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Job Email": {
      "main": [
        [
          {
            "node": "Needs LLM Fallback?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Needs LLM Fallback?": {
      "main": [
        [
          {
            "node": "Classify With LLM",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Classified?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify With LLM": {
      "main": [
        [
          {
            "node": "Apply LLM Fallback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apply LLM Fallback": {
      "main": [
        [
          {
            "node": "Classified?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classified?": {
      "main": [
        [
          {
            "node": "Write to Inbox",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Write to Needs Review",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write to Inbox": {
      "main": [
        [
          {
            "node": "Loop Feed Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Loop Feed Rows": {
      "main": [
        [
          {
            "node": "Upsert Feed",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Find Feed Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Find Feed Row": {
      "main": [
        [
          {
            "node": "Merge Into Feed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Merge Into Feed": {
      "main": [
        [
          {
            "node": "Loop Feed Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upsert Feed": {
      "main": [
        [
          {
            "node": "Mark Email Processed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write to Needs Review": {
      "main": [
        [
          {
            "node": "Mark Email Processed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Ingest Backlog Check": {
      "main": [
        [
          {
            "node": "Find Stale Job Mail",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Find Stale Job Mail": {
      "main": [
        [
          {
            "node": "Stale Job Mail Alarm",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveExecutionProgress": true
  },
  "pinData": {},
  "meta": {
    "instanceId": "job-tracker-dashboard"
  },
  "tags": []
}
